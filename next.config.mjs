/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: [
            "cdn.openai.com",
            "oaidalleapiprodscus.blob.core.windows.net"        
        ]
    }
};

export default nextConfig;
